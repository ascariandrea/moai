<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ascariandrea.afw.samples">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LauncherActivity_"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            LOGIN ACTIVITY

            Remember to declare the com.facebook.LoginActivity
            to complete the facebook login.
        -->
        <activity android:name=".login.LoginActivity_"
            android:label="@string/login">
        </activity>
        <activity android:name="com.facebook.LoginActivity" />

        <!--
            FACEBOOK APP ID
            You need to change this facebook app id with yours.
        -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/fb_app_id" />


    </application>

</manifest>
